version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: sudkul/reverseproxy:v5
  backend_user:
    build:
      context: ./udagram-api-user
    image: sudkul/udagram-api-user:v5
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: sudkul/udagram-api-feed:v5
  frontend:
    build:
      context: ./udagram-frontend
    image: sudkul/udagram-frontend:v5
